import dotenv from 'dotenv';

dotenv.config();

export const generateInterventionCards = async (
  geminiApiKey: string,
  userContext: any
): Promise<any> => {
  // Placeholder for calling Gemini API using standard fetch or SDK
  console.log('generateInterventionCards helper called');
  return {
    interventions: []
  };
};
